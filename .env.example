# Factory Automation System - Secrets and API Keys Only
# All configuration options are in config.yaml

# Required API Keys
OPENAI_API_KEY=sk-...
TOGETHER_API_KEY=...  # For Qwen2.5VL vision model

# Gmail OAuth Credentials (optional - for Gmail integration)
GMAIL_CLIENT_ID=your-client-id
GMAIL_CLIENT_SECRET=your-client-secret

# Database Passwords (optional - defaults provided)
DATABASE_PASSWORD=your-secure-password
REDIS_PASSWORD=  # Leave empty if Redis has no password

# Security Secret Key (required - generate a secure random key)
SECRET_KEY=your-very-secure-secret-key-here

# Note: All other configuration options (ports, intervals, feature flags, etc.)
# should be set in config.yaml, not here.